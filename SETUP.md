# คู่มือติดตั้ง — สนามคำศัพท์

เกมเล่นได้ทันทีตั้งแต่ตอนนี้แล้ว (เปิดไฟล์ `index.html`)
คู่มือนี้คือขั้นตอนทำให้มัน **ขึ้นเว็บจริง + แข่งกันข้ามเครื่องได้**

| ขั้น | ใช้เวลา | ใครทำ | ได้อะไร |
|---|---|---|---|
| 1. กระดานคะแนนกลาง (Supabase) | ~10 นาที | **คุณ** (ต้องสมัครบัญชี) | คุณกับแฟนเล่นคนละเครื่องแต่เห็นคะแนนกัน |
| 2. ขึ้นเว็บฟรี (GitHub Pages) | ~10 นาที | **คุณ** (ต้องสมัครบัญชี) | ได้ลิงก์จริง ส่งให้เพื่อนเปิดในมือถือได้ |
| 3. เอาคำศัพท์จาก Sheet เข้าเกม | ~2 นาที ต่อครั้ง | คุณหรือผม | คลังคำอัปเดตตามที่ Gemini ทำ |

> **ทำไมผมทำสองขั้นแรกแทนไม่ได้:** ทั้งสองอย่างต้องสมัครบัญชีและใส่รหัสผ่าน
> ซึ่งเป็นสิ่งที่ผมทำแทนคุณไม่ได้ โค้ดทั้งหมดผมเขียนให้แล้ว
> คุณแค่ก๊อบคีย์มาวางในไฟล์เดียว ถ้าติดตรงไหนถ่ายรูปหน้าจอมาถามได้

---

## ขั้นที่ 1 — กระดานคะแนนกลาง (Supabase)

Supabase คือฐานข้อมูลออนไลน์ แผนฟรีเหลือเฟือสำหรับเกมนี้
(ฟรีจริง ไม่ต้องผูกบัตร รองรับ 500MB และ 50,000 ผู้ใช้ต่อเดือน — เกมนี้ใช้ไม่ถึง 1%)

### 1.1 สมัครและสร้างโปรเจกต์

1. เข้า **https://supabase.com** → กด `Start your project`
2. สมัครด้วยบัญชี GitHub หรืออีเมลก็ได้
3. กด `New project`
   - **Name:** `vocab-arena`
   - **Database Password:** กดปุ่มสุ่มแล้วกด copy เก็บไว้ที่ไหนสักที่ (จะไม่ได้ใช้ในเกม แต่เก็บไว้เผื่อ)
   - **Region:** เลือก `Southeast Asia (Singapore)` — ใกล้ไทยที่สุด เร็วสุด
4. กด `Create new project` แล้วรอประมาณ 2 นาที

### 1.2 สร้างตารางเก็บคะแนน

1. เมนูซ้ายมือ กด **`SQL Editor`** (ไอคอนคล้ายกระดาษมีตัวอักษร SQL)
2. กด `New query`
3. เปิดไฟล์ **`tools/supabase.sql`** ในโฟลเดอร์นี้ ก๊อบทั้งไฟล์
4. วางลงในช่อง แล้วกด **`Run`** (หรือ Ctrl+Enter)
5. ถ้าขึ้นข้อความเขียวว่า `Success. No rows returned` = เรียบร้อย

### 1.3 ก๊อบคีย์มาใส่ในเกม

1. เมนูซ้ายล่าง กด **`Project Settings`** (รูปเฟือง) → **`Data API`**
2. จะเห็นสองอย่างที่ต้องใช้

   | ในหน้า Supabase | ก๊อบไปวางที่ |
   |---|---|
   | **Project URL** เช่น `https://abcdefgh.supabase.co` | `SUPABASE_URL` |
   | **Project API Keys → `anon` `public`** (ยาวมาก) | `SUPABASE_KEY` |

3. เปิดไฟล์ **`config.js`** ด้วย Notepad แล้วแก้สองบรรทัดบนสุด

```javascript
  SUPABASE_URL: "https://abcdefgh.supabase.co",
  SUPABASE_KEY: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9....",
```

   ⚠️ อย่าลืมเครื่องหมาย `"` ครอบ และ `,` ท้ายบรรทัด

   > URL จะก๊อบมาแบบมี `/rest/v1/` ต่อท้ายหรือไม่มีก็ได้ โค้ดตัดให้เอง

4. เซฟไฟล์ แล้วเปิดเกมใหม่ — ป้ายมุมขวาบนต้องเปลี่ยนจาก `เก็บในเครื่อง` เป็น **`กระดานกลาง`** สีเขียว

### เรื่องความปลอดภัย

คีย์ `anon public` ออกแบบมาให้เปิดเผยในหน้าเว็บได้ ไม่ใช่ความลับ
ตารางถูกตั้งค่าให้ **อ่านได้ + เพิ่มคะแนนใหม่ได้ แต่แก้หรือลบของคนอื่นไม่ได้**
อย่างแย่ที่สุดคือมีคนยิงคะแนนปลอมเข้ามา ซึ่งลบทิ้งได้ด้วยคำสั่งท้ายไฟล์ `supabase.sql`

**ห้ามเอาคีย์ที่ชื่อ `service_role` มาใส่เด็ดขาด** — อันนั้นเป็นคีย์ผู้ดูแลระบบ

**Database Password ที่ตั้งตอนสร้างโปรเจกต์ก็เป็นความลับ** เกมไม่ได้ใช้เลย
เก็บไว้ในโปรแกรมจดรหัสผ่านเท่านั้น อย่าส่งให้ใครและอย่าพิมพ์ลงในแชท
ถ้าหลุดไปแล้วให้เข้า `Project Settings` → `Database` → `Reset database password`

### ลบคะแนนทดสอบทิ้ง

ตอนลองระบบมักมีแถวขยะค้างอยู่ ลบได้ที่ SQL Editor

```sql
delete from public.scores where name = 'ทดสอบระบบ';
```

---

## ขั้นที่ 2 — ขึ้นเว็บฟรีด้วย GitHub Pages

### 2.1 สมัครและสร้างที่เก็บ

1. เข้า **https://github.com** สมัครบัญชี (ฟรี)
2. กดปุ่ม `+` มุมขวาบน → `New repository`
   - **Repository name:** `vocab-arena`
   - เลือก **`Public`** (จำเป็น — GitHub Pages ฟรีต้องเป็น Public)
   - **ไม่ต้อง**ติ๊ก Add a README
3. กด `Create repository`

### 2.2 อัปโหลดไฟล์

1. ในหน้าที่เพิ่งสร้าง กด **`uploading an existing file`**
2. เปิดโฟลเดอร์ `vocab-arena` ในเครื่อง เลือก**ทุกอย่างข้างใน** แล้วลากมาวางในหน้าเว็บ
   - ต้องมี: `index.html`, `style.css`, `app.js`, `config.js`, โฟลเดอร์ `data`
   - โฟลเดอร์ `tools` กับไฟล์ `.md` จะอัปหรือไม่อัปก็ได้ ไม่มีผลกับเกม
   - ⚠️ ลากตัว**ไฟล์** ไม่ใช่ลากโฟลเดอร์ `vocab-arena` ทั้งอัน ไม่งั้นลิงก์จะมีคำว่า vocab-arena ซ้ำสองชั้น
3. กด `Commit changes`

### 2.3 เปิด Pages

1. แท็บ **`Settings`** ด้านบน → เมนูซ้าย **`Pages`**
2. หัวข้อ **Build and deployment → Source** เลือก `Deploy from a branch`
3. **Branch:** เลือก `main` และโฟลเดอร์ `/ (root)` → กด `Save`
4. รอ 1–2 นาที รีเฟรชหน้า จะมีกล่องเขียวขึ้นลิงก์

```
https://ชื่อบัญชีคุณ.github.io/vocab-arena/
```

**นั่นคือลิงก์เกม** ส่งให้เพื่อนกับแฟนได้เลย เปิดในมือถือได้ทันที ไม่ต้องติดตั้งอะไร

> **เคล็ดลับ:** ในมือถือ เปิดลิงก์แล้วกดเมนู → `เพิ่มไปยังหน้าจอโฮม` จะได้ไอคอนเหมือนแอปจริง

### 2.4 เวลาจะแก้อะไรทีหลัง

เข้า repo → กดที่ไฟล์ที่จะแก้ → รูปดินสอ ✏️ → แก้ → `Commit changes`
รอ 1 นาทีเว็บอัปเดตเอง

---

## ขั้นที่ 3 — เอาคำศัพท์จาก Google Sheet เข้าเกม

พอ Gemini ทำคำศัพท์ใน Sheet เสร็จแต่ละแท็บ

1. ใน Sheet เปิดแท็บนั้น → `ไฟล์` → `ดาวน์โหลด` → `ค่าที่คั่นด้วยจุลภาค (.csv)`
2. เปลี่ยนชื่อไฟล์เป็นชื่อระดับ แล้ววางในโฟลเดอร์ `tools/csv/`
   ```
   tools/csv/starter.csv
   tools/csv/middle.csv
   tools/csv/toeic.csv
   tools/csv/advanced.csv
   ```
3. เปิด Command Prompt ที่โฟลเดอร์ `vocab-arena` แล้วรัน

```bash
python tools/build-data.py
```

สคริปต์จะแปลงข้อมูล **พร้อมตรวจข้อผิดพลาดให้ด้วย** โดยเฉพาะเรื่องความหมายซ้ำในหมวดเดียวกัน
ซึ่งเป็นบั๊กที่ทำให้บางข้อมีตัวเลือกถูก 2 อัน

4. ไฟล์ใน `data/` จะถูกอัปเดต เอาขึ้น GitHub ทับของเดิม

> **ไม่อยากรันสคริปต์เอง?** ส่งไฟล์ CSV มาให้ผม ผมแปลงและตรวจให้ แล้วส่งไฟล์ที่พร้อมใช้กลับไป

---

## ปัญหาที่พบบ่อย

**ป้ายขึ้น `ต่อไม่ได้` สีเทา**
- ตรวจว่าใน `config.js` ใส่ URL กับ KEY ครบ มี `"` ครอบ และไม่มีช่องว่างเกิน
- ตรวจว่ารันไฟล์ `tools/supabase.sql` ใน SQL Editor แล้ว
- กด F12 ในเบราว์เซอร์ ดูแท็บ Console จะมีข้อความบอกสาเหตุ

**เปิด index.html แล้วหน้าว่างเปล่า / ไม่มีระดับให้เลือก**
- เกิดจากเปิดไฟล์ตรงๆ แบบ `file://` ซึ่งเบราว์เซอร์ห้ามโหลดไฟล์ข้อมูลด้วยเหตุผลด้านความปลอดภัย
- แก้โดยขึ้น GitHub Pages ตามขั้นที่ 2 หรือถ้าจะทดสอบในเครื่องให้รัน
  ```bash
  python -m http.server 8000
  ```
  แล้วเปิด `http://localhost:8000`

**คะแนนไม่ขึ้นกระดาน**
- คะแนน 0 จะไม่ถูกบันทึก
- กระดานแยกตาม ระดับ + โหมด ดูให้ตรงกับที่เล่น
- โหมดท้าดวลจะเทียบเฉพาะคนที่ใช้**รหัสเดียวกัน**

**อยากล้างกระดานเริ่มแข่งรอบใหม่**
- Supabase → SQL Editor → รัน `truncate public.scores;`

---

## สรุปว่าใครทำอะไร

| งาน | สถานะ |
|---|---|
| หน้าเกม ระบบเล่น 4 ระดับ 3 โหมด | ✅ ผมทำเสร็จแล้ว |
| ระบบตั้งค่า (สี ขนาดอักษร เวลา เสียง) | ✅ ผมทำเสร็จแล้ว |
| โค้ดกระดานคะแนนกลาง + สคริปต์ฐานข้อมูล | ✅ ผมทำเสร็จแล้ว |
| ตัวแปลง CSV → ข้อมูลเกม พร้อมตัวตรวจ | ✅ ผมทำเสร็จแล้ว |
| บรีฟงานคลังคำสำหรับ Gemini | ✅ ผมทำเสร็จแล้ว (`GEMINI-BRIEF.md`) |
| สมัคร Supabase + ก๊อบคีย์มาวาง | ⬜ คุณ (~10 นาที) |
| สมัคร GitHub + อัปไฟล์ + เปิด Pages | ⬜ คุณ (~10 นาที) |
| คลังคำ 4,000 คำ | ⬜ Gemini + คุณตรวจ |
